# Pin to older version that still has solutions API
mediapipe==0.10.21

# Install opencv-python-headless FIRST (before mediapipe)
# This prevents mediapipe from forcing opencv-contrib-python
opencv-python-headless

# Other dependencies
streamlit
pillow
numpy
rembg
onnxruntime
